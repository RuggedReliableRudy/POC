name: Deploy CpeLoad Infrastructure

on:
  workflow_run:
    workflows: ["Build CpeLoad Image"]
    types: [completed]

env:
  AWS_REGION: us-gov-west-1

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read
      actions: read   # needed to download artifacts

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ secrets.AWS_GOVCLOUD_ROLE_ARN }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts

      - name: Load ECR URI + Image Tag
        run: |
          echo "ECR_URI=$(cat ecr.txt | cut -d '=' -f2)" >> $GITHUB_ENV
          echo "IMAGE_TAG=$(cat image.txt | cut -d '=' -f2)" >> $GITHUB_ENV

      - name: Terraform Init
        run: terraform init

      - name: Terraform Apply (deploy ECS)
        run: terraform apply -auto-approve -var="image_tag=${IMAGE_TAG}" -var="ecr_uri=${ECR_URI}"

